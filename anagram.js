export function isAnagram(string, anagram) {
    // TODO
    // takes two parameters: string and anagram
    // returns true if parameter anagramecontains stringthe same letters as 
    // the given string with the same counters of occurrences

    // принимает два параметра: строку и анаграмму
    // возвращает true, если анаграмма параметра содержит строку с теми же буквами, что и 
    // заданная строка с одинаковыми счетчиками вхождений

    // advanced false
    return false;
}